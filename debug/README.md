# debug/

One-off scripts used during development for testing individual components,
debugging data issues, and verifying fixes. Not part of the production codebase.

These files are safe to ignore. The official test suite lives in `backend/tests/`.
